module.exports=async(req,res)=>{res.setHeader('Cache-Control','public, max-age=60, s-maxage=60');const asset=String(req.query?.asset||'').toLowerCase(),amount=Number(req.query?.amount||0),map={bitcoin:['bitcoin','BTC'],litecoin:['litecoin','LTC'],usdt:['tether','USDT']};if(!map[asset]){res.statusCode=400;return res.end(JSON.stringify({error:'Unsupported asset'}))}try{const [id,symbol]=map[asset],r=await fetch('https://api.coingecko.com/api/v3/simple/price?ids='+id+'&vs_currencies=usd'),d=await r.json(),rate=Number(d?.[id]?.usd);if(!rate)throw Error();res.setHeader('Content-Type','application/json');res.end(JSON.stringify({rate,symbol,amount:amount?(amount/rate).toFixed(8):null}))}catch{res.statusCode=502;res.setHeader('Content-Type','application/json');res.end(JSON.stringify({error:'Crypto rate unavailable'}))}};
